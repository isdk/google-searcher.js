bug:
  ☐ 模拟人类行为失败：目前直接使用 `fill` 模拟操作和简单的 `waitFor` 延迟仍无法绕过 Google 的反爬虫检测。
    - 即使使用 `browser` 引擎，Google 依然能识别出自动化脚本。
    - 可能原因：Google 可能在检测底层的浏览器指纹、真实的鼠标/键盘事件轨迹，或者对请求头、TLS 指纹有特殊校验。
    - 下步行动：需要深入研究 Google 的反爬虫（anti-bot）机制，判断是基于行为分析还是环境检测。可能需要使用更低层级的事件模拟（如 `keyboard.type` 而非 `fill`）或增强指纹伪装。
bing:
  ☐ Bing 网页搜索结果摘要中通常不包含日期信息。
    - 现状：简单的 CSS 选择器无法从搜索结果页获取日期。
    - 想法：如果需要精确日期，可能需要跟进每个结果的 URL，通过请求 HEAD 或解析目标页面的 meta 信息来获取。
